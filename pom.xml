<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.yourserver</groupId>
    <artifactId>adaptation</artifactId>
    <version>10.30</version>
    <packaging>jar</packaging>

    <properties>
        <!-- Minecraft 26.2 (и Paper, и Purpur) требует Java 25: см. -->
        <!-- https://fill.papermc.io/v3/projects/paper/versions/26.2 → java.version.minimum = 25 -->
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <repository>
            <id>momirealms-repo</id>
            <url>https://repo.momirealms.net/releases/</url>
        </repository>

        <repository><id>plasmoverse</id><url>https://repo.plasmoverse.com/releases</url></repository>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>su.plo.voice.api</groupId><artifactId>server</artifactId><version>2.1.16</version><scope>provided</scope>
        </dependency>
        
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <!--
              Целевая версия сервера: Minecraft 26.2 — Paper 26.2 build 129
              (Purpur 26.2 — форк Paper, тот же API, отдельный purpur-api не нужен).

              Схема координат с 26.1 изменилась: вместо 1.21.11-R0.1-SNAPSHOT
              Paper публикует готовые версии вида 26.2.build.<номер>-stable
              (см. maven-metadata в https://repo.papermc.io/repository/maven-public/).
            -->
            <version>26.2.build.129-stable</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.11.4</version>
            <scope>test</scope>
        </dependency>
        <!--
          CraftEngine 26.8.x — линия, собранная под Paper 26.2: в её
          gradle.properties paper_version=26.2.build.+ и
          latest_supported_version=26.2. Линия 26.9 уже идёт на 26.3, поэтому
          на Purpur 26.2 держим 26.8.2.
        -->
        <dependency>
            <groupId>net.momirealms</groupId>
            <artifactId>craft-engine-core</artifactId>
            <version>26.8.2</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>net.momirealms</groupId>
            <artifactId>craft-engine-bukkit</artifactId>
            <version>26.8.2</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <defaultGoal>clean package</defaultGoal>
        <sourceDirectory>src/main/java</sourceDirectory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>25</source>
                    <target>25</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
            </plugin>
        </plugins>
    </build>
</project>
